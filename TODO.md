# TODO

## できたこと

- [x] ビルドが遅い
- [x] 履歴がたくさんあると、j/kで移動してもスクロールいないので貼り付けたい行が見えない
- [x] 一覧の幅は半分でいい。その代わり長い行は最大4行で表示したい。4行でも表示しきれなければ...で省略でいい
- [x] 画面の下端や右端の方で一覧を出すと一覧画面の一部が画面の外に出てしまうことがある
- [x] j/kだけじゃなくカーソルキーでも移動可能にしたい
- [x] カーソル位置に一覧を表示しなくていい。前回表示した位置に表示でいい（初期位置は画面中央）。一覧はマウスで移動可能にしてほしい。一覧の幅は倍にしてほしい。
- [x] 同じ内容のものが追加された場合、既存のものを一覧の一番上に移動してほしい
- [x] dd で削除したなら、p とかで貼り付けたくなるのが人情
- [x] yy や Y でコピーして p とかで貼り付けたくなるのが人情
- [x] 一覧の行の余白が多すぎる。もっとコンパクトにしたい
- [x] 一覧でctrl+cでクリップボードにコピーしたい
- [x] 検索中も一覧上で移動したい。ctrl+n/pで移動
- [x] 一覧のウインドウをリサイズ可能にしたい
- [x] 連続貼り付けに対応したい
- [x] 一覧のj/kとかでの移動は先頭・末尾でとめて
- [x] ctrl+[もescとして使えるようにして
- [x] キー操作でウインドウ移動・リサイズさせたい
- [x] 一覧の先頭9件に `1`〜`9` を表示し、そのキーで貼り付けて閉じる。`Ctrl+1`〜`9` は貼り付けて残す
- [x] `t` / `T` でタグを付け外し。`V` 選択中は範囲の全行に効く
- [x] `.` で直前の変更（`dd`・`p`/`P`・`t`/`T`・`m`）を繰り返す
- [x] `u` で一覧の変更を取り消す。`Ctrl+R` でやり直す
- [x] `V` で複数選択。`Enter` で改行区切りに連結して貼り付け
- [x] `E` で nvim（または `$EDITOR`）を開いて編集。終了したら読み戻す
- [x] `m` でピン留めして先頭に残す
- [x] 登録時に URL・複数行・パスっぽいものへ自動でタグを付ける
- [x] 貼り付け先のアプリごとに履歴を上に出す。ブラウザはページ単位
- [x] `Shift+Enter` で空白・改行・引用符を整形して貼り付ける
- [x] `{{date}}` や `{{time}}` を貼り付け時に展開する
- [x] 貼り付け後にクリップボードの元のテキストを戻す
- [x] 一覧の位置とサイズを settings.json に残す
- [x] `Ctrl+4` で前面の選択テキストを登録する
- [x] `{{clip}}` / `{{n}}` / `{{uuid}}` / `{{user}}` / `{{host}}` / `{{date:%Y%m%d}}` を展開する
- [x] `#run` と `{{sh:}}` と本文スクリプト、`#file` でファイル内容を貼る
- [x] `Ctrl+Shift+1`〜`9` で一覧を出さずに貼る
- [x] `gf` / `o` / `Space` / `Tab` タグ切替 / `:help` / `:sh` / `:@` / `:export` / `:clear`
- [x] `#secret` / `#tmp` / `#alias:foo` / 貼った回数の並び
- [x] `{{sel}}` / `{{ask:}}` / `#type` / `gEnter` / `#once`
- [x] `a` / `#here` / `f;` / `J`・`gJ` / `+` `-`
- [x] `:` 履歴 / `:import` / `S` / `u` スタックと `Ctrl+R`

## やりたいこと

### 貼り付け

- [ ] `g` のあと `T` は `V` 中なら選んだ行をタブ区切りで 1 行にして貼って閉じる。`V` でなければ何もしない。`J` は空白、`gJ` はカンマ、`Enter` は改行のまま
- [ ] `#quote` が付いた行は、貼るとき各行の先頭に `> ` を付ける。すでに `>` で始まっている行はそのまま。このタグは `Ctrl+4` の自動タグでは付けない。`Shift+Enter` の整形より後に効く
- [ ] `{{app}}` を前面アプリのプロセス名、`{{front}}` をウィンドウタイトルへ置き換える。取れなければ空文字。展開は `#run` / `#file` より先。履歴の本文は書き換わらない
- [ ] `{{pick: prod, stg, dev}}` を含む行を貼るときは、貼る前に一覧の下へ候補を出す。`j` / `k` で選んで Enter で確定。Esc / Ctrl+[ は貼り付け中止（一覧は閉じない）。候補はカンマ区切りで、前後の空白は落とす。候補が 1 つも無ければ空文字。同じトークンが複数あっても 1 回だけ聞く。`{{ask:}}` と両方あるときは `{{ask:}}` を先に全部聞いてから `{{pick:}}`
- [ ] `#tsv` が付いた行は、展開後の本文をタブ区切りにして貼る。本文が JSON の配列（文字列か、同じキーのオブジェクト）ならその値。それ以外は 1 行 1 値と見て、行をタブでつなぐ。JSON が壊れそうなときや配列が空なら貼り付けを中止する。このタグは自動では付けない。`#run` と両方あるときは `#run` が先
- [ ] `#log:C:\notes\out.md` のようにパスを付けた行は、前面へ貼らず、展開後の本文をそのファイルの末尾へ追記して閉じる。ファイルが無ければ作る。書けなければ中止。`#run` と両方あるときは `#run` が先で、その標準出力を追記する。タグに空白は使えないのでパスに空白があるときは使えない
- [ ] `gp` で直前に貼った行をもう一度貼る（同じ ids、同じつなぎ方）。`.` は一覧の変更用のまま混ぜない。貼っていないとき、失敗したとき、`#once` で消えたあとは何もしない。アプリ終了で忘れる

### 操作

- [ ] `M` は `V` 中なら選んだ行を改行で 1 行にまとめる。タグは和集合（重複なし）。ピンは 1 つでも付いていれば残す。元の行は消す。`S` の逆。`.` の対象。`V` でなければ何もしない
- [ ] `c` で選択行をすぐ下に複製する（新しい id、本文とタグとピンをコピー、回数は 0）。`V` 中は範囲ごと、元の並びのまま下へ。`.` の対象
- [ ] `g?` で選択行の貼った回数・貼り付け先・タグを一覧の上に出す。もう一度 `g?` で閉じる。`Space` の全文とは別。`j` / `k` で移動したらその行の情報に切り替わる。貼り付けはしない
- [ ] `#not` が付いた行は、その行が覚えている貼り付け先が前面のときは一覧に出さない。貼り付け先が空の行は今どおり出す。`#here` と両方あるときは `#here` だけ効く。このタグは自動では付けない
- [ ] `:s/old/new` で選択行の本文の `old` を全部 `new` に置き換えて保存する。正規表現は使わない。`old` が空、または `/` が足りないときは何もしない。`V` 中は範囲。`.` の対象
- [ ] `Ctrl+D` で一覧を半ページ下、`Ctrl+U` で半ページ上。端で止まる。選択行も一緒に動かす
- [ ] ファイルを一覧へドロップしたら、パスを本文にして `#path` と `#file` を付けた行を先頭に作る。フォルダでもよい。常時監視はしない
- [ ] `:dedup` のあと `:dedup yes` で、前後の空白と改行だけ違う行を 1 つにまとめる。残すのはピンがある方、どちらもピンなら新しい方。消した行は `u` で戻せる

### 寿命

- [ ] `#ttl:1h` が付いた行は、その時間が過ぎていたら起動時に消す。単位は `m` / `h` / `d` だけ（`30m` `1h` `1d`）。登録した時刻から数える。パースできなければ普通のタグとして残して消さない。`#tmp` は今どおり次回起動で必ず消す

### あとまわし

- [ ] ブラウザの貼り付け先をウィンドウタイトルではなく URL で覚えたい。UI Automation が要るので様子見

## やらないこと

- `I` で選択範囲を履歴本文に置き換えること。アプリによって効かないのでやらない
- `3dd` や `5j` のカウント接頭辞。`1`〜`9` を直接貼り付けに使っているので入れられない。繰り返しは `.` で足りる
- 本文のコードフェンス（三つバッククォートで囲んだブロック）を実行の目印にすること。コピー元の文章に普通に混ざるので、意図しない実行の経路になる
- `"a yy` で a に、`"a p` で a から貼る名前付きレジスタ。`"` の次の 1 文字（`a`〜`z`）を待つ。`"` を付けなければ今までのレジスタ。アプリ終了で忘れる
- 全角半角・ひらがなカタカナを正規化して検索したい
- ローマ字検索で日本語に当てたい
- `Ctrl+4` で登録するとき、本文が JWT（`eyJ` で始まるドット区切り）、`-----BEGIN `、`sk-`、`ghp_`、`github_pat_` のどれかに当たれば `#secret` を自動で付ける。外すのは今どおり `T`
