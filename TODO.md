# TODO

## 機能追加や修正後に必ず行うこと

* READMEへの追記・修正
* help の追記・修正
  * help に記載する粒度は既存の内容に合わせること
* which-key の追記・修正

## できたこと

- [x] ビルドが遅い
- [x] 履歴がたくさんあると、j/kで移動してもスクロールいないので貼り付けたい行が見えない
- [x] 一覧の幅は半分でいい。その代わり長い行は最大4行で表示したい。4行でも表示しきれなければ...で省略でいい
- [x] 画面の下端や右端の方で一覧を出すと一覧画面の一部が画面の外に出てしまうことがある
- [x] j/kだけじゃなくカーソルキーでも移動可能にしたい
- [x] カーソル位置に一覧を表示しなくていい。前回表示した位置に表示でいい（初期位置は画面中央）。一覧はマウスで移動可能にしてほしい。一覧の幅は倍にしてほしい。
- [x] 同じ内容のものが追加された場合、既存のものを一覧の一番上に移動してほしい
- [x] dd で削除したなら、p とかで貼り付けたくなるのが人情
- [x] yy や Y でコピーして p とかで貼り付けたくなるのが人情
- [x] 一覧の行の余白が多すぎる。もっとコンパクトにしたい
- [x] 一覧でctrl+cでクリップボードにコピーしたい
- [x] 検索中も一覧上で移動したい。ctrl+n/pで移動
- [x] 一覧のウインドウをリサイズ可能にしたい
- [x] 連続貼り付けに対応したい
- [x] 一覧のj/kとかでの移動は先頭・末尾でとめて
- [x] ctrl+[もescとして使えるようにして
- [x] キー操作でウインドウ移動・リサイズさせたい
- [x] 一覧の先頭9件に `1`〜`9` を表示し、そのキーで貼り付けて閉じる。`Ctrl+1`〜`9` は貼り付けて残す
- [x] `t` / `T` でタグを付け外し。`V` 選択中は範囲の全行に効く
- [x] `.` で直前の変更（`dd`・`p`/`P`・`t`/`T`・`m`）を繰り返す
- [x] `u` で一覧の変更を取り消す。`Ctrl+R` でやり直す
- [x] `V` で複数選択。`Enter` で改行区切りに連結して貼り付け
- [x] `E` で nvim（または `$EDITOR`）を開いて編集。終了したら読み戻す
- [x] `m` でピン留めして先頭に残す
- [x] 登録時に URL・複数行・パスっぽいものへ自動でタグを付ける
- [x] 貼り付け先のアプリごとに履歴を上に出す。ブラウザはページ単位
- [x] `Shift+Enter` で空白・改行・引用符を整形して貼り付ける
- [x] `{{date}}` や `{{time}}` を貼り付け時に展開する
- [x] 貼り付け後にクリップボードの元のテキストを戻す
- [x] 一覧の位置とサイズを settings.json に残す
- [x] `Ctrl+4` で前面の選択テキストを登録する
- [x] `{{clip}}` / `{{n}}` / `{{uuid}}` / `{{user}}` / `{{host}}` / `{{date:%Y%m%d}}` を展開する
- [x] `#run` と `{{sh:}}` と本文スクリプト、`#file` でファイル内容を貼る
- [x] `Ctrl+Shift+1`〜`9` で一覧を出さずに貼る
- [x] `gf` / `o` / `Space` / `Tab` タグ切替 / `:help` / `:sh` / `:@` / `:export` / `:clear`
- [x] `#secret` / `#tmp` / `#alias:foo` / 貼った回数の並び
- [x] `{{sel}}` / `{{ask:}}` / `#type` / `gEnter` / `#once`
- [x] `a` / `#here` / `f;` / `J`・`gJ` / `+` `-`
- [x] `:` 履歴 / `:import` / `S` / `u` スタックと `Ctrl+R`
- [x] `gT` / `g>` `:quote` / `gp`
- [x] `{{app}}` / `{{front}}` / `{{pick:}}` / `#tsv` / `#log:path` / `#ttl:1h`
- [x] `M` / `c` / `g?` / `#not` / `:s` / `Ctrl+D` `Ctrl+U` / ドロップ / `:dedup`
- [x] `:` の Tab 補完と `g` / `d` / `y` / `f` の which-key
- [x] `:map` / `:unmap` / `:mapleader`
- [x] `{{@foo}}` / `{{env:USERPROFILE}}`
- [x] `:sort`
- [x] `E` のメモ帳フォールバック / `:settings` / `:set paste` / `target_keys`
- [x] `:type`
- [x] `:set a="text"` / `{{var:a}}`
- [x] `:echo`
- [x] 選んだ `{{date}}` / `:sh` / `:echo` を Ctrl+Shift+H で置き換える
- [x] `:help template` の `{{xx}}` 一覧
- [x] `{{var a}}`（`:` の代わりに空白）
- [x] `#app:chrome` / `#not:chrome`（`#here` / `#not` はやめる）
- [x] 回数順と貼り付け先での自動上げをやめる。ピンの下は新しい順
- [x] ピンを `gp` にする。直前の貼り付けは `g.`（`m` はやめる）
- [x] `#multi` を自動で付けない
- [x] `J` をまとめ（旧 `M`）。空白つなぎ貼りはやめる
- [x] 検索中の Tab で一覧へ。`Esc` / `Ctrl+[` で絞りを外す
- [x] `{{tag:work}}` / `{{type:<Tab>}}`
- [x] `:set a=` で変数を消す / `:tags` / `{{date-1d}}` / `{{wait:}}` / `ge` と Ctrl+C の展開 / `#run` 確認
- [x] `:n 100` で `{{n}}` の初期値。貼ったときだけ進む
- [x] 貼ったあと一覧を出すと、選んでいた行に戻る
- [x] `{{var: {{var: b}}}}` / `#lock` で削除防止
- [x] `#slot:3` / `{{pick tag:env}}` / `#confirm` / `ga` / `{{date+1w}}` `{{date+1m}}`
- [x] `{{sel|clip}}` 空なら隣
- [x] `:set a+=1` 貼って成功したあとに残る連番
- [x] `{{pick:}}` の候補が 9 個までなら `1`〜`9` で確定
- [x] `:format` / `:raw` / `:join` / `:open`（旧 Shift+Enter / g Enter / gJ / gT / gf）
- [x] `:quote` に行頭の印を渡せる。未指定は `> `。空白は `:quote "* "`
- [x] `:join` で区切り指定。未指定は `,`。タブは `:join "\\t"`。`:comma` / `:tab` は別名
- [x] 末尾 `> clip` で結果をクリップボードへ
- [x] `:.!sh xxx` でカレント行を stdin に渡す（`:sh` は stdin なしのまま）
- [x] `:!!sh xxx` で選択行をフィルタして本文を書き換える
- [x] `{{when chrome}}` で貼るとき前面アプリに合わせて本文を切り替える（`{{when app: chrome}}` に直す）
- [x] Ubuntu（Wayland）。貼るときはクリップボードに置いて閉じる。キー注入と前面アプリ判定はしない


- [x] `{{when}}` は種類を揃える。アプリは `{{when app: chrome}}`。複数は `{{when app: chrome, msedge}}`。足すのは `var` と `focus`。書き方は `{{when app: chrome}}` `{{when var: a: "AAA"}}` `{{when focus: Edit}}`。`{{when chrome}}` はやめる（残したままなら展開せずそのまま）。種類はこれ以外にしない。1本の鎖で、先に当たった枝だけ残す。種類が混ざってよい。どれにも当たらないときは `{{when}}`。それも無ければ空。先頭の `{{when` より前は残す。`{{when var: a: "AAA"}}` は `:set a=` のそのままの文字列と比べる。値は `"` で括る。`"` 自体は `\"`。例: `:set a="AAA"` のとき `{{when var: a: "AAA"}}git{{when var: a: "BBB"}}hg{{when}}none` は `git`。`{{when app: chrome}}web{{when focus: Edit}}{{date}}{{when}}{{date:%Y-%m-%d}}` は Chrome なら `web`、メモ帳の入力欄なら `2026/09/22`、どれでもなければ `2026-09-22`。`{{when focus: Edit}}` は `{{focus}}` と同じ種別名（`Edit` `Document` `ComboBox`）。Ubuntu では `app` と `focus` は空なので当たらず、`var` と `{{when}}` だけ使う
- [x] `{{pick}}` の種類を `{{when}}` と同じ形にする。列挙は `{{pick list: prod, stg, dev}}`。`{{pick: prod, stg, dev}}` はやめる（残したままなら空）。タグは `{{pick tag: env}}`。検索は `{{pick search: "xx"}}`。検索は本文が `/` と同じ当たりの行を候補にする。自分自身は入れない。当たる行が無ければ聞かない。検索文字列は `"` で括る。`"` 自体は `\"`
- [x] `{{nop: メモ}}` は貼るとき消える。中のトークンは展開しない。`|` と `}}` は書かない
- [x] 本文がパイプなら、貼る操作はどれもそのパイプを実行する。Enter / `Ctrl+Enter` / `1`〜`9` / `Ctrl+1`〜`9` / `Ctrl+Shift+1`〜`9`。いまは本文をテンプレートとして貼るだけなので、本文が `:sel | upper` の行を Enter しても `:sel | upper` という文字が貼られる。例: `#slot:1` の本文が `:sel | snake`。メモ帳で `getUserName` を選んで `Ctrl+Shift+1` すると、一覧は出ずに `get_user_name` に置き換わる。同じ行を一覧で Enter しても同じ。本文が `:sel | upper` なら `hello` が `HELLO`。`:sel | quote "> " | clip` なら選んだ各行の頭に `> ` を付けてクリップボードへ置き、前面には貼らない。本文がパイプでなければ今までどおり本文を貼る。段の名前が不正、または `sel` が空なら何もしない。`Ctrl+Shift+H` は対象外（選んだ `:sel | snake` は計算しない。今どおり `{{date}}` / `:sh` / `:echo` だけ）。`{{tag:slot:1}}` を `Ctrl+Shift+H` で展開するのも対象外（スロットの本文を貼るだけで、パイプとしては走らない）
- [x] `#grab` の穴は `<名前>` にする。`{{ }}` は使わない。例: 1行目 `https://github.com/<org>/<repo>/pull/<pr>`、2行目 `gh pr checkout <pr> --repo <org>/<repo>`。前面で `https://github.com/hatamon/hataclip/pull/12` を選んで Enter すると `gh pr checkout 12 --repo hatamon/hataclip` が貼られる。`https://example.com` なら何も貼らない。入力が `error at src/a.rs:12` と `warn at src/b.rs:44` と `error at src/c.rs:3` の3行で、1行目が `error at <file>:<line>`、2行目が `<file>:<line>` なら `src/a.rs:12` と `src/c.rs:3` を貼る。2行目が無ければ何もしない。名前は `:set` と同じ識別子。`<` `>` は穴の括弧にだけ使う。穴は直後の文字の直前まで（直後が行末なら残り全部）。0文字、または穴が改行を含むときは不一致。入力に `\n` があれば行ごとに照合し、当たった行だけ2行目を `\n` でつなぐ。どれも当たらなければ何もしない。捕らえた値は `:set` にも `settings.json` にも残さない。Windows は `Ctrl+C` で選択を読む。Ubuntu はクリップボードを照合する。空なら何もしない
- [x] `split` / `col` / `get` は、表や JSON から1つの値を抜く。例: 前面の選択が `a,b,c` のとき `:sel | split , | col 2` は `b` を貼る（1列目が `a`）。クリップボードが `{"items":[{"name":"hatamon"}]}` のとき `:clip | json | get /items/0/name | show` はヘルプ画面に `hatamon` と出す。`split` の区切りは必須。タブは `split "\\t"`。分けた列はタブ1つでつなぎ直す。`col` は足りない行を捨て、1未満や整数でない引数は何もしない。`get` は JSON Pointer。読めない、またはポインタが無いときは何もしない。文字列は引用符なし。数値・`true`・`false`・`null` はその字。オブジェクトと配列は空白なしの JSON
- [x] `diff` / `only` は、2つの文章の行を比べる。例: クリップボードが `a` と `b` の2行、選択行が `b` と `c` の2行のとき、`:clip | diff . | show` はヘルプに `- a` と `+ c` を出す（共通の `b` は出さない）。`:. | only clip | show` は選択にあってクリップボードに無い行なので `c` だけ出す。引数は `.` か `clip` だけ。全部同じ、または `only` が0行なら何もしない
- [x] `:quote` は行末も付けられる。引数が1つ、または未指定のときは今までどおり行頭だけ（未指定は `> `。空白は `:quote "* "`）。引数が2つのときは両方 `"` で括り、1つ目が行頭、2つ目が行末。`:quote "> " "<"` は `hello` を `> hello<` にする。`"` 自体は `\"`。1つ目が `""` なら行頭は付けない。2つ目が `""` なら行末は付けない。各行について、行頭は末尾空白を除いた印で既に始まっていれば重ねない。行末は先頭空白を除いた印で既に終わっていれば重ねない。印が空白だけなら、その空白そのもので比べる。片方だけ足りなければ足りないほうだけ足す。空行にも付ける。パイプの段も同じ書き方
- [x] いま選んでいる文字に使える行を、一覧の上に出す。例: ブラウザで `https://github.com/hatamon/hataclip/pull/12` を選んでから一覧を開くと、`#grab` の1行目がその URL に当たる行がピンの直下に来て、プレビューには `gh pr checkout 12 --repo hatamon/hataclip` が出ている。Enter でそれを貼る。本文が `:sel | upper` のようなパイプの行はその次に来て、プレビューには前面の選択を通した結果（`HELLO`）を出す。この並べ替えでプレビューを作るときは `sh` の段を実行しない（`sh` を含む行は並べ替えの対象にせず、今の順のまま）。どちらでもない行はその下に今の順で残る。履歴の本文は書き換えない。一覧を出す直前に前面へ `Ctrl+C` を送り、200ms 後のクリップボードを読んで、すぐ `Ctrl+C` の前へ戻す。選択が空なら今までどおり新しい順。Ubuntu はキーを送らないので並べ替えない
- [x] `{{cred:github}}` は、貼るときに Windows の資格情報マネージャーからその名前のパスワードを読む。`#secret` は今どおり行を隠すタグで、資格情報とは別。例: 本文が `{{cred:github}}` の行を Enter するとパスワードそのものが貼られる。履歴ファイルと `settings.json` に残るのは `{{cred:github}}` という文字だけ。名前は `:set` と同じ識別子。無ければ貼らない。`ge` と `Ctrl+C` では展開しない（`{{cred:github}}` のまま残す）。プレビューは `••••`。貼った 200ms 後にクリップボードを戻すのは今の貼り付けと同じ。Ubuntu は読まないので、このトークンがある行は貼らない
- [x] 作った式を行に残して、あとで別の入力に効かせる。例: 前面で `userName` を選び `:sel | kebab | add` すると、一覧に本文 `user-name` の行ができて、別の欄に `sel | kebab` が残る（タグにはしない）。次に `HTTPResponse` を選んでその行で `g:` を押すと、本文が `http-response` に変わる。式が残るのは `| add` と `:!!sh` で本文ができた行。`:from` でその式だけを編集する（`e` は今までどおり本文だけ）。`g:` は式をもう一度実行する。`sel` はいまの前面の選択、`.` はいまの一覧の行。失敗したら本文はそのまま。`.` の対象。`u` で戻せる。式の無い行で `g:` は何もしない
- [x] `{{focus}}` は `{{app}}` と同じく、貼るときの値だけ。フォーカスしている入力欄の種類（UI Automation の名前そのまま。`Edit` `Document` `ComboBox`）。取れなければ空。常時監視はしない。Ubuntu は空。本文の切り替えは `{{when focus: Edit}}`（上の `{{when}}`）。例: メモ帳の入力欄で `{{focus}}` を貼ると `Edit`
- [x] パイプの入力元を揃える。`.` は一覧の選択行だけ（`V` は改行つなぎ。展開しない）。`sel` は前面の選択。Windows では実行前に前面へ `Ctrl+C` を送り、200ms 後のクリップボードを読んで、すぐ `Ctrl+C` の前へ戻す。空なら何もしない。Ubuntu はキーを送らないので `sel` を含むパイプは実行しない。`clip` はまだ流れが無いときだけクリップボードを読む。末尾の `| clip` だけが書くほう。途中の `clip`（流れがあるあとの `clip`）は不正で、パイプ全体を何もしない。例: `:sel | upper` は前面の `hello` を `HELLO` にして貼る。`:. | upper` は一覧の行を大文字にして貼る。`:clip | show` はクリップボードをヘルプに出す
- [x] 段の名前は `|` が無くても同じ動き。`:upper` は選択行を大文字にして貼る（`:upper |` の行き先なしと同じ）。`:json | show` も `:json` も可。`:echo 3+4` は今までどおりで、`:echo 3+4 | show` も同じ段。1段だけでもパイプとして実行する
- [x] `:set` の `"` も `:quote` と同じ。`\"` は `"`、`\t` はタブ、`\n` は改行。例: `:set a="say \"hi\""` の `{{var:a}}` は `say "hi"`
- [x] `:comma` と `:tab` はやめる。区切りは `:join ","` と `:join "\\t"`。残したままなら何もしない
- [x] 末尾の `> clip` はやめる。書くのは `| clip` だけ。`quote "* " > clip` は不正で何もしない
- [x] `Ctrl+4` を2秒以内に2回続けると、1回目を入力、2回目を出力として式を1つ推測し、先頭に仮の行を置く。Enter で残す。Esc でその行を消す。常時監視はしない。2秒を過ぎていたら今までどおり1件登録。推測は次だけ。入力を1段で変換すると出力に一致するなら `:sel | snake` のようにその段（候補は `camel` `pascal` `snake` `kebab` `upper` `lower`。複数一致ならこの順で先）。一致が無ければ、入力と出力の共通するリテラルを残し、違う連続部分を左から `<a>` `<b>` `<c>` … にした `#grab` 行（1行目が入力の型、2行目が出力）。共通リテラルが1文字も無い、穴が0個、または `sh` が要るときは何もしない（2回目は今までどおり1件登録）。例: 1回目 `getUserName`、2回目 `get_user_name` → 本文 `:sel | snake`。1回目 `https://github.com/hatamon/hataclip/pull/12`、2回目 `gh pr checkout 12 --repo hatamon/hataclip` → `#grab` で `<a>` `<b>` `<c>` が org / repo / 番号
- [x] 段に `put` を足す。いまの流れを JSON として読み、JSON Pointer の位置へ値を書いて、その JSON を出す。`get` の逆。例: 前面が `{"name":"x","n":1}` のとき `:sel | put /n 2 | json` は `{"name":"x","n":2}` を整形して貼る。引数はポインタ、空白、値。値は `:quote` と同じ引用（`put /n "2"`）。引用しなければ空白の後ろ全部。値が JSON として読めたらその値（数値・`true`・`false`・`null`・オブジェクト・配列）、読めなければ文字列。流れが JSON でない、またはポインタが無いときは何もしない
- [x] `:` の入力中、ヘルプの位置に結果の先頭12行を出す。入力は開いたときに読んだ前面の選択（`sel`）。それが空なら一覧の選択行（`.`）。最後のキーから 50ms 後に更新。12行を超えたら末尾に `...`。`sh` を含む、または段が不正、または `json` が読めない、または必要な入力が空のときはプレビューを出さない（空）。Enter で実行するのは今どおり。例: 前面が `helloWorld` のとき `:sel | kebab` と打つと `hello-world` が見える
- [x] `g~` は `#grab` の1行目と2行目を入れ替える。3行目以降はそのまま。穴 `<名前>` はそのまま。`#grab` が無い、または2行目が無いときは何もしない。`.` の対象。`u` で戻せる。例: 1行目が URL の型、2行目が `gh pr checkout …` の行で `g~` すると、コマンドから URL を組み立てる行になる
- [x] 段に `each` を足す。これより後ろの段を、流れを `\n` で分けた各行に対して独立に実行し、成功した行だけを `\n` でつなぐ。末尾の `\n` は空行を1つ足さない。`each` より前は塊のまま。当たる行が0なら何もしない。`each` の後ろに `sh` があったら不正で、パイプ全体を何もしない。例: 前面が `{"id":1}` と `{"id":2}` の2行のとき `:sel | each | get /id` は `1` と `2` を貼る。1行目だけ壊れていたら `2` だけ。`#grab` の複数行は上のとおり行ごと（`each` は不要）
- [x] 履歴で語を補完するショートカットを足す。展開（初期値 `Ctrl+Shift+H`）とは別キー。初期値 `Ctrl+9`。Settings で変える。展開・登録・表示と同じキーなら登録しない。一覧は出さない。前面の選択を `Ctrl+C` で読む（今の展開と同じ）。空なら何もしない。`Ctrl+A` は送らない。当たる行は一覧を `/` でその選択語で絞ったときと同じ（`#secret` は除く）。無ければ何もしない。1行なら本文全部、複数行なら当たった最初の行だけを、テンプレートは展開せず貼る。例: 履歴に `hata007@corp.jp` と `hata007-admin` があるとき、`hata007` を選んで1回目は `hata007@corp.jp`。2秒以内にもう一度押すと、前面へ `Ctrl+Z` を送ってから次の `hata007-admin` を貼る。3回目はまた `hata007@corp.jp`。2秒を過ぎたら、そのときの選択が新しい語。Undo が効かない前面では2回目以降は何もしない。Ubuntu はキーを送らないので、候補をクリップボードへ置くだけ。2秒以内の連打はクリップボードの中身を次の候補にする
- [x] `hataclip` をパイプの式を引数1つに取る CLI にする。シェルの `|` と式の `|` は別。式は `"` か `'` で括る。先頭の `:` はあってもなくても同じ。標準入力がパイプならそれが流れの最初（一覧の `.` は使わない）。行き先が無ければ標準出力。前面には貼らない。例: `git log -5 | hataclip quote` は引用符付きのログを画面に出す。`git log -5 | hataclip 'quote "> " | clip'` はクリップボードへ書いて画面には出さない。`git diff | hataclip add` は標準入力を一覧へ1件（`Ctrl+4` と同じ自動タグ。クリップボードは触らない）。`hataclip 'echo 3+4'` は入力が無くても `7` を出す。壊れた JSON、知らない段、入力が要るのに空なら何も出さず終了コード 1。`| show` は標準出力と同じなので書かない。引数が無く標準入力もパイプでなければ今までどおり一覧を出す。式があるときはウィンドウを開かず終わったらプロセスも終わる。`| add` と `| set` は起動中の hataclip と同じ履歴と変数を触る
- [x] `:help` を、名前の羅列ではなく「打つと何が起きるか」まで書く。いまの `:help colon` と `:help template` は1行ずつの説明で、例が無い。各 `:` コマンドと各 `{{ }}` に、打つ文字、前面／一覧／クリップボードのどれが変わるか、失敗したときの動き、Ubuntu で効かないならその旨、入力と結果の例を付ける。例: `:quote "* "` は選択行の `hello` が `* hello` になって前面へ貼る。履歴は触らない。すでに `* ` で始まっていればそのまま。`:join` は `V` していなければ何もしない。`{{sel|clip}}` は選択が空ならクリップボード、それも空なら空。`:help quote` のようにコマンド名でも出す（いまは `ない: quote`）。パイプは `:help colon` に、入力元 `.` `clip` と行き先 `clip` `add` `set` `open` `show` と段の例 `:sh dir | quote "> " | clip` を書く。機能を足すときは同じ形で help も足す。README の表は今の短さのままでよい
- [x] パイプの先頭に入力元を書ける。`:. | sh xxx` の `.` はいまの選択行（`V` は改行つなぎ。テンプレートは展開しない。`:.!sh xxx` と同じ結果）。`:clip | show` の `clip` はクリップボードを読む（末尾の `| clip` は今までどおり書くほう）
- [x] 段に `camel` / `pascal` / `snake` / `kebab` / `upper` / `lower` を足す。名前の変換は `_` `-` 空白と大文字の変わり目で区切る。`upper` / `lower` は文字だけ変える
- [x] 段に `json` / `xml` を足す。整形して出す。読めなければ何もしない
- [x] `|` で `:` コマンドを左からつなぐ。`:sh dir | sh sort | quote "x " | clip` は、最初の `sh` は stdin なし、次の `sh` は左の結果を stdin に受け取る。`:quote` は各行の先頭に付ける。行き先は末尾の1つ。`| clip` はクリップボード（前面には貼らない。今の `> clip` はこれにする）。`| add` は一覧へ1件（改行は残す。`Ctrl+4` と同じ自動タグ。クリップボードは触らない）。`| set a` は変数へ残す。`| open` は URL かパスなら開く。`| show` はヘルプ画面に出す。`"` の中の `|` では切らない
- [x] Ctrl+Shift+H で、選択が2行以上かつ1行目が `:` で始まるパイプなら、その式を2行目以降に実行して選択全体を結果に置き換える。1行目は結果に残さない。`:` で始まらない、または段として読めない1行目は、今どおり選択全体の `{{date}}` / `:sh` / `:echo` だけ。1行だけの `:echo` と `:sh` も今どおり。1行だけの `:s/old/new` は2行目が無いので何もしない。例: 次の3行を選ぶと `abc def new ghi` と `newabc ddd` に置き換わる。`:quote | upper` なら `xxx` と `yyy` が `> XXX` と `> YYY`。`:. | s/old/new` も同じで、`.` と `sel` は2行目以降。流れは最初からあるので、1行目の `:sh` はそれを stdin に受け取る（一覧で打つ最初の `:sh` は今どおり stdin なし）。確認は出さない。履歴は変えない。新しいキーは足さない。`Ctrl+C` は今の展開と同じ1回で、読んだあとクリップボードを戻す。式の中の `sel` のために送り直さない。`s/old/new` は段としても足す。`:sel | s/old/new` は前面の選択の `old` を全部 `new` にして貼る。区切りは `/`。正規表現は使わない。`old` が空なら何もしない。`new` は2つ目の `/` の後ろ全部。一覧で打つ `:s/old/new` は今どおり履歴の本文を書き換える。選択の式で `old` が残りに無くても、残りをそのまま置く。2行目が無い、1行目が読めない、段が不正、選択が空、`sh` が失敗、なら何もしない。`| clip` `| show` `| add` `| set` `| open` は今のパイプと同じ。`:help` の Ctrl+Shift+H もこの動きに合わせる。Ubuntu はキーを送らないのでクリップボードを読み、結果はクリップボードに置く

```
:s/old/new
abc def old ghi
oldabc ddd
```

- [x] 一覧を出したあと `:` を押しても、一覧を隠さない。`sel` のプレビューのために `Ctrl+C` を送らない。隠れているあいだに打った `h` や `e` が落ちる。プレビューの `sel` は、一覧を開いたときに読んだ選択を使う。それが空なら一覧の行。Enter で `sel` が要るパイプを実行するときは、今どおりその場で読む。クリップボードの読み取り（`clip`）は今どおり隠さない

- [x] `:help s` は説明が2つある。出るのは先のほうだけで、`u` で戻せることは書いていない。1つにする。一覧で打つ `:s/old/new` は本文を書き換え、`u` で戻せる。段の `:sel | s/old/new` は前面を置換して貼る。Ctrl+Shift+H の複数行は2行目以降を置換して選択全体を置き換える。書き方は今の `:help quote` と同じ（打つ文字、何が変わるか、失敗、例）。README の表は短いまま

- [x] `:help colon` と `:help sh` と `:help sel` を、一覧のパイプと Ctrl+Shift+H の複数行で同じ事実にする。一覧の最初の `:sh` は stdin なし。複数行の1行目の `:sh` は2行目以降を stdin に受け取る。`.` と `sel` は、一覧では一覧の行と前面の選択、複数行ではどちらも2行目以降。Ubuntu では履歴の行にある `sel` は実行しない。Ctrl+Shift+H の複数行では選択はもう読んであるので `sel` があっても実行し、結果はクリップボードに置く

- [x] 複数行の1行目が `:echo` だけのときは、下の行を捨てて計算結果にしない。1行だけの `:echo 2+3` は今どおり `5`。例: `:echo 2+3` の次の行が `notes` なら何もしない（`notes` は残る）

- [x] 前面の選択の読み取りを揃える。パイプの `sel` と一覧を開くときと同じく、Ctrl+C の 200ms 後を読み、クリップボードが変わらなければ空。Ctrl+Shift+H と `{{sel}}` は今 70ms で、コピーに失敗しても今のクリップボードを選択として扱う。空なら何もしない

- [x] 置換の結果が空でも、選択を空に置き換える。いま `:s/old/` で全部消えると、空の貼り付けを捨てるので元の文字が残る。例: 前面の `old` に `:sel | s/old/` を掛けると選択は空。Ctrl+Shift+H の複数行も同じ。履歴の本文を `:s/old/new` で空にするのは今どおりで、`u` で戻せる

- [x] Ctrl+Shift+H の1行でも、`sh` か `echo` が自分で結果を作るパイプは実行して選択を置き換える。最初の `:sh` は stdin なし（一覧と同じ）。例: `:sh dir | quote` は `dir` の各行の頭に `> `。`:echo 2+3 | quote ">" "<"` は `>5<`。`:sh "xx | yy" | quote` はシェルが `xx | yy` を実行してから各行に `> `。引用符の外の `|` は段。段が読めなければ何もしない（`:sh dir | sort` はシェルに渡さない。シェルのパイプは `:sh "dir | sort"`）。入力が要る1行（`:quote` `:s/old/new` `:sel | upper` `:. | upper`）は何もしない。`sel` `clip` `.` を読む1行も何もしない。2行以上は今どおり（1行目が式、残りが入力。1行目が `:echo` だけなら何もしない）。履歴は変えない。新しいキーは足さない。失敗、空、選択が空なら何もしない。`| clip` `| show` `| add` `| set` `| open` は今のパイプと同じ。Ubuntu は結果をクリップボードに置く

- [x] 段に `filter` を足す。含む行だけ残す。正規表現は使わない。大文字と小文字は区別する。例: `a.txt` と `b.rs` に `:filter ".txt"` で `a.txt`。`:filter not ".txt"` は含まない行なので `b.rs`。`:sh dir | filter ".txt"` は `dir` のうち当たる行。括りは `:quote` と同じ。`"` で括る。`"` は `\"`、`\t` はタブ、`\n` は改行。括らなければ空白の後ろ全部。`not` は針の前の単語だけ。針が `not` なら `:filter "not"`。`not` の後ろが空、引数が空、流れが空、1行も残らないときは何もしない。選択は空にしない。`| quote` や `| clip` はその後ろに続く。一覧の `:filter txt` は選択行に掛ける（`:upper` と同じ）。Ctrl+Shift+H の1行は `sh` か `echo` が先にあるときだけ。`:filter` だけの1行は何もしない。2行以上は1行目の式を下の行に掛ける。Ubuntu でも動く

- [x] 一覧で打つ `:s/old/new` は `:quote` と同じく、選んだ行から貼る文字を作るだけにする。いまは履歴の本文を書き換えて前面には貼らない。変えたいのは、選択行の `old` を全部 `new` にした文字を前面へ貼り、履歴は触らないこと。正規表現は使わない。`old` が空なら何もしない。`old` が無くても残りを貼る。結果が空でも選択は空にする（いまの段と同じ）。`V` ならその範囲を改行つなぎにしてから置換する（`:upper` と同じ）。段の `:sel | s/old/new` は今どおり前面の選択を置換して貼る。履歴は触らない。Ctrl+Shift+H の複数行は今どおり（1行目が `:s/old/new` なら2行目以降を置換して選択全体を置き換える。履歴は触らない）。1行だけの `:s/old/new` を Ctrl+Shift+H しても何もしない（入力が要るので `:quote` と同じ）。`.` の対象から外す（いまは本文の書き換えを繰り返す）。`u` では戻らない。本文を直すのは `e`。`:help s` と README の表も `:quote` と同じ形にする

- [x] `:` を開いたとき、選択行が見えるまで一覧をスクロールする。入力欄とプレビューで一覧が短くなっても、いまの行が画面の外に残らない。`V` なら範囲の先頭。選択が1行ならその行

- [x] Windows は exe を分ける。トレイは `hataclip-gui.exe`（コンソールなし。引数は見ない。ダブルクリックで常駐）。パイプは `hataclip.exe`（コンソール付き。毎回打つので短い名前。式を1つ。`dir | hataclip "quote | clip"` はクリップボード。`hataclip "echo 3+4"` は `7`。式が無い、または2つ以上は終了コード 1）。Linux は `hataclip` 1本でトレイもパイプも行う。同じ本文の `| add` は2件目を作らず、既存の行を先頭へ移す

- [x] `:set copy ctrl+shift+c` で、いまの前面アプリのコピーキーを書く。`:set paste` と同じ。`Ctrl+4` と実行時の `sel` がそのキーを送る。一覧を開くときは送らない。`copy` は変数名にできない

- [x] 一覧を開くときにコピーキーを送らない。当たった `#grab` を先頭へ出さない。`sel` のプレビューも出さない。Enter の `sel` と `#grab` は今どおり、その場で選択を読む

## やりたいこと

- [x] 流れがあるところの `clip` は、その文字をクリップボードへ書いて、同じ文字を次の段へ渡す。失敗にしない。例: `dir | hataclip "clip | upper"` は `dir` の出力をクリップボードへ置き、大文字にしたものを標準出力へ出す。`:sel | clip | upper` は前面の選択をクリップボードへ置き、大文字にしたものを前面へ貼る。流れが無い `hataclip "clip | upper"` は今どおりクリップボードを読んで大文字にする。末尾の `| clip` は今どおり、そこまでの結果を書いて終わり。空ならクリップボードは変えない

- [x] 流れがあるところの `add` も同じ。その文字を一覧へ1件足して、同じ文字を次の段へ渡す。例: `dir | hataclip "add | upper"` は `dir` の出力を一覧へ足し、大文字にしたものを標準出力へ出す。`:sel | kebab | add | quote` は `user-name` を一覧へ足し（残る式は `sel | kebab`）、`> user-name` を前面へ貼る。末尾の `| add` は今どおり足して終わり、画面にも前面にも出さない。空なら足さない。同じ本文は先頭へ移すだけで、2件目は作らない。流れが無い途中の `add` は足す文字が無いので、パイプ全体を何もしない

- [ ] 一覧を出しているあいだの `hataclip add` は、閉じなくてもすぐその行を出す。そのあと一覧で削除やタグ変更をしても、足した行を消さない。開いたときに持っていた古い中身で `items.json` を上書きしない。`Ctrl+4` の2回目でできた仮の行を、一覧を閉じるときに消すときも同じ。同じ本文は今どおり先頭へ移すだけで、2件目は作らない

### あとまわし

## やらないこと

- `I` で選択範囲を履歴本文に置き換えること。アプリによって効かないのでやらない
- `3dd` や `5j` のカウント接頭辞。`1`〜`9` を直接貼り付けに使っているので入れられない。繰り返しは `.` で足りる
- 本文のコードフェンス（三つバッククォートで囲んだブロック）を実行の目印にすること。コピー元の文章に普通に混ざるので、意図しない実行の経路になる
- `"a yy` で a に、`"a p` で a から貼る名前付きレジスタ。`"` の次の 1 文字（`a`〜`z`）を待つ。`"` を付けなければ今までのレジスタ。アプリ終了で忘れる
- 全角半角・ひらがなカタカナを正規化して検索したい
- ローマ字検索で日本語に当てたい
- `Ctrl+4` で登録するとき、本文が JWT（`eyJ` で始まるドット区切り）、`-----BEGIN `、`sk-`、`ghp_`、`github_pat_` のどれかに当たれば `#secret` を自動で付ける。外すのは今どおり `T`
- `#os:windows` / `#os:linux` で OS ごとに行を出すこと。`#app:` と `{{when}}` で足りる
- `#clip` で Enter をクリップボード置きにすること。末尾 `| clip` で足りる
- `#page:GitHub` でウィンドウタイトルのページ名に絞ること。`#app:` と検索で足りる
- `gx`。開く系は `:` コマンド（と必要なら `:map`）で足りる
- 貼り付けキュー（`V` の Enter で 1 件ずつ順に貼る）。今は改行つなぎで足りる
- `#key:` で行ごとにグローバルショートカット。`#slot:n` と `:map` で足りる
- `ma` / `'a` のジャンプ用マーク。検索とピンで足りる
- `yi"` `yi'` `yi(` `yi)` `yi{` `yi}`。内側だけヤンクするのはやらない。`yy` で行ごとで足りる
- ブラウザの貼り付け先をウィンドウタイトルではなく URL で覚えたい。UI Automation が要るので様子見
