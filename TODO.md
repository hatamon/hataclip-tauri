# TODO

## できたこと

- [x] ビルドが遅い
- [x] 履歴がたくさんあると、j/kで移動してもスクロールいないので貼り付けたい行が見えない
- [x] 一覧の幅は半分でいい。その代わり長い行は最大4行で表示したい。4行でも表示しきれなければ...で省略でいい
- [x] 画面の下端や右端の方で一覧を出すと一覧画面の一部が画面の外に出てしまうことがある
- [x] j/kだけじゃなくカーソルキーでも移動可能にしたい
- [x] カーソル位置に一覧を表示しなくていい。前回表示した位置に表示でいい（初期位置は画面中央）。一覧はマウスで移動可能にしてほしい。一覧の幅は倍にしてほしい。
- [x] 同じ内容のものが追加された場合、既存のものを一覧の一番上に移動してほしい
- [x] dd で削除したなら、p とかで貼り付けたくなるのが人情
- [x] yy や Y でコピーして p とかで貼り付けたくなるのが人情
- [x] 一覧の行の余白が多すぎる。もっとコンパクトにしたい
- [x] 一覧でctrl+cでクリップボードにコピーしたい
- [x] 検索中も一覧上で移動したい。ctrl+n/pで移動
- [x] 一覧のウインドウをリサイズ可能にしたい
- [x] 連続貼り付けに対応したい
- [x] 一覧のj/kとかでの移動は先頭・末尾でとめて
- [x] ctrl+[もescとして使えるようにして
- [x] キー操作でウインドウ移動・リサイズさせたい
- [x] 一覧の先頭9件に `1`〜`9` を表示し、そのキーで貼り付けて閉じる。`Ctrl+1`〜`9` は貼り付けて残す
- [x] `t` / `T` でタグを付け外し。`V` 選択中は範囲の全行に効く
- [x] `.` で直前の変更（`dd`・`p`/`P`・`t`/`T`・`m`）を繰り返す
- [x] `u` で一覧の変更を取り消す。`Ctrl+R` でやり直す
- [x] `V` で複数選択。`Enter` で改行区切りに連結して貼り付け
- [x] `E` で nvim（または `$EDITOR`）を開いて編集。終了したら読み戻す
- [x] `m` でピン留めして先頭に残す
- [x] 登録時に URL・複数行・パスっぽいものへ自動でタグを付ける
- [x] 貼り付け先のアプリごとに履歴を上に出す。ブラウザはページ単位
- [x] `Shift+Enter` で空白・改行・引用符を整形して貼り付ける
- [x] `{{date}}` や `{{time}}` を貼り付け時に展開する
- [x] 貼り付け後にクリップボードの元のテキストを戻す
- [x] 一覧の位置とサイズを settings.json に残す
- [x] `Ctrl+4` で前面の選択テキストを登録する
- [x] `{{clip}}` / `{{n}}` / `{{uuid}}` / `{{user}}` / `{{host}}` / `{{date:%Y%m%d}}` を展開する
- [x] `#run` と `{{sh:}}` と本文スクリプト、`#file` でファイル内容を貼る
- [x] `Ctrl+Shift+1`〜`9` で一覧を出さずに貼る
- [x] `gf` / `o` / `Space` / `Tab` タグ切替 / `:help` / `:sh` / `:@` / `:export` / `:clear`
- [x] `#secret` / `#tmp` / `#alias:foo` / 貼った回数の並び
- [x] `{{sel}}` / `{{ask:}}` / `#type` / `gEnter` / `#once`
- [x] `a` / `#here` / `f;` / `J`・`gJ` / `+` `-`
- [x] `:` 履歴 / `:import` / `S` / `u` スタックと `Ctrl+R`
- [x] `gT` / `g>` `:quote` / `g*` `:bullet` / `gp`
- [x] `{{app}}` / `{{front}}` / `{{pick:}}` / `#tsv` / `#log:path` / `#ttl:1h`
- [x] `M` / `c` / `g?` / `#not` / `:s` / `Ctrl+D` `Ctrl+U` / ドロップ / `:dedup`
- [x] `:` の Tab 補完と `g` / `d` / `y` / `f` の which-key
- [x] `:map` / `:unmap` / `:mapleader`
- [x] `{{@foo}}` / `{{env:USERPROFILE}}`
- [x] `:sort`
- [x] `E` のメモ帳フォールバック / `:settings` / `:set paste` / `target_keys`
- [x] `:type`
- [x] `:set a="text"` / `{{var:a}}`
- [x] `:echo`
- [x] 選んだ `{{date}}` / `:sh` / `:echo` を Ctrl+Shift+H で置き換える
- [x] `:help template` の `{{xx}}` 一覧
- [x] `{{var a}}`（`:` の代わりに空白）
- [x] `#app:chrome` / `#not:chrome`（`#here` / `#not` はやめる）
- [x] 回数順と貼り付け先での自動上げをやめる。ピンの下は新しい順
- [x] ピンを `gp` にする。直前の貼り付けは `g.`（`m` はやめる）
- [x] `#multi` を自動で付けない
- [x] `J` をまとめ（旧 `M`）。空白つなぎ貼りはやめる
- [x] 検索中の Tab で一覧へ。`Esc` / `Ctrl+[` で絞りを外す
- [x] `{{tag:work}}` / `{{type:<Tab>}}`
- [x] `:set a=` で変数を消す / `:tags` / `{{date-1d}}` / `{{wait:}}` / `ge` と Ctrl+C の展開 / `#run` 確認
- [x] `:n 100` で `{{n}}` の初期値。貼ったときだけ進む
- [x] 貼ったあと一覧を出すと、選んでいた行に戻る
- [x] `{{var: {{var: b}}}}` / `#lock` で削除防止
- [x] `#slot:3` / `{{pick tag:env}}` / `#confirm` / `ga` / `{{date+1w}}` `{{date+1m}}`
- [x] `{{sel|clip}}` 空なら隣
- [x] `:set a+=1` 貼って成功したあとに残る連番
- [x] `{{pick:}}` の候補が 9 個までなら `1`〜`9` で確定
- [x] `:format` / `:raw` / `:join` / `:open`（旧 Shift+Enter / g Enter / gJ / gT / gf）
- [x] `:quote` に行頭の印を渡せる。未指定は `> `。空白は `:quote "* "`。`:bullet` は別名
- [x] `:join` で区切り指定。未指定は `,`。タブは `:join "\\t"`。`:comma` / `:tab` は別名
- [x] 末尾 `> clip` で結果をクリップボードへ
- [x] `:.!sh xxx` でカレント行を stdin に渡す（`:sh` は stdin なしのまま）
- [x] `:!!sh xxx` で選択行をフィルタして本文を書き換える
- [x] `{{when chrome}}` で貼るとき前面アプリに合わせて本文を切り替える
- [x] Ubuntu（Wayland）。貼るときはクリップボードに置いて閉じる。キー注入と前面アプリ判定はしない

## やりたいこと

- [ ] `{{whenvar: a: "AAA"}}` は `:set` の値で本文を切り替える。`{{when}}` と同じで、先に当たった枝だけ残す。`{{whenvar}}` はどれにも当たらないとき。比べるのは `:set` のそのままの文字列。値は `"` で括る。`"` 自体は `\"`
- [ ] `{{pick: search:"xx"}}` は本文が `/` と同じ検索に当たる行を候補にする。自分自身は入れない。当たる行が無ければ聞かない。検索文字列は `"` で括る。`"` 自体は `\"`
- [ ] `{{nop: メモ}}` は貼るとき消える。中のトークンは展開しない。`|` と `}}` は書かない
- [ ] `Ctrl+Shift+1`〜`9` の行がパイプ（`|` が2段以上。先頭の `:` はあってもなくても同じ）なら、本文を貼らずそのパイプを実行する。`|` が無ければ今までどおり本文を貼る。段の名前が不正なら何もしない。一覧の Enter と `Ctrl+1`〜`9` は対象外。パイプが `.` を読むとき（先頭の `.`、または `diff` / `only` の引数 `.`）は、実行前に前面へ `Ctrl+C` を送り、200ms 後のクリップボードを `.` のテキストにする。読んだ直後、`Ctrl+C` の前のクリップボードへ戻す。パイプ中の `clip` はその戻した内容。`.` が空なら実行しない。Ubuntu はキーを送らないので、`.` を読むパイプは実行しない。行き先が無ければ結果を前面へ貼り、貼った 200ms 後にクリップボードを戻すのは今の貼り付けと同じ。`| clip` はクリップボードへ書いて前面には貼らない
- [ ] `#grab` の本文は、1行目が型、2行目以降が貼る文。2行目が無ければ何もしない。1行目の `{{名前}}` は穴。名前は `:set` と同じ識別子で、組み込みトークン名（`date` `clip` `sel` `n` など）は不可。不可なら何もしない。穴は直後のリテラルの先頭1文字の直前まで（直後が行末なら残り全部）。0文字、または改行を含むときは不一致。`|` のフォールバックは型では使わない。照合は1行だけ。Windows では前面へ `Ctrl+C` を送り、200ms 後のクリップボードを読み、すぐ `Ctrl+C` の前へ戻す。空なら何もしない。Ubuntu はキーを送らないので、クリップボードのテキストを照合する。空なら何もしない。当たったら2行目以降の同じ `{{名前}}` を、捕らえた文字列のまま置き（再展開しない）、そのあと通常のトークンを展開して貼る。捕らえた値は `:set` に残さない。`settings.json` にも書かない。当たらなければ貼らない。一覧からなら一覧は開いたまま。成功したときの閉じる／残すは Enter / `Ctrl+Enter` / `Ctrl+Shift+1`〜`9` の今の規則
- [ ] 段に `split` / `col` / `get` を足す。`split` の区切りは必須。`:join` と同じ引用（`split ", "`、タブは `split "\\t"`）。各行をその区切りで分け、列をタブ1つでつなぎ直す。連続する区切りは空の列。引用符は区切りの一部として割らない。`col 3` は各行をタブで分け、3列目（1始まり）だけ残す。列が足りない行は捨てる。1行も残らなければ何もしない。1未満や整数でない引数は何もしない。`get /items/0/name` はいまの流れを JSON として読み、JSON Pointer（RFC 6901。`~0` は `~`、`~1` は `/`）の値を出す。読めない、またはポインタが無いときは何もしない。文字列は引用符なしの中身。数値・`true`・`false`・`null` はその字。オブジェクトと配列は空白なしの JSON。引数なしの `get` は文書全体
- [ ] 段に `diff` / `only` を足す。引数は `.` か `clip` だけ。それ以外は何もしない。左はいまの流れ（まだ無ければ、先頭の `.` と同じく選択行。`raw` が先なら展開しない）。右は引数。`.` は先頭に書いた `.` と同じテキスト。行は `\n` 区切り。末尾の `\n` は空行を1つ足さない。`diff` は共通の最長部分列に入らない行だけ出す。左の行は `- `、右の行は `+ `。並びは元の位置のまま。全部同じなら何もしない。`only` は右に同じ行が1つでもあれば左から全部除き、残った行を左の順で出す（左の重複は残す）。0行なら何もしない
- [ ] `yi"` `yi'` `yi(` `yi)` `yi{` `yi}` は、カーソル行の本文から内側だけを `yy` と同じレジスタに入れる。`V` の範囲は見ない。`V` は解除しない。行は増えない。クリップボードは変えない。前面には貼らない。`p` / `P` で一覧へ置くときタグは空。`"` と `'` は、先頭から最初の開きの次から、`\"` `\'` を1文字として飛ばした対応する閉じの直前まで。`(` と `)` は丸括弧、`{` と `}` は波括弧。どちらで書いても同じ組。先頭から見て、最初に閉じまで揃った組。深さは数える。揃う前の閉じは無視。閉じが無ければレジスタは変えない
- [x] パイプの先頭に入力元を書ける。`:. | sh xxx` の `.` はいまの選択行（`V` は改行つなぎ。テンプレートは展開しない。`:.!sh xxx` と同じ結果）。`:clip | show` の `clip` はクリップボードを読む（末尾の `| clip` は今までどおり書くほう）
- [x] 段に `camel` / `pascal` / `snake` / `kebab` / `upper` / `lower` を足す。名前の変換は `_` `-` 空白と大文字の変わり目で区切る。`upper` / `lower` は文字だけ変える
- [x] 段に `json` / `xml` を足す。整形して出す。読めなければ何もしない
- [x] `|` で `:` コマンドを左からつなぐ。`:sh dir | sh sort | quote "x " | clip` は、最初の `sh` は stdin なし、次の `sh` は左の結果を stdin に受け取る。`:quote` は各行の先頭に付ける。行き先は末尾の1つ。`| clip` はクリップボード（前面には貼らない。今の `> clip` はこれにする）。`| add` は一覧へ1件（改行は残す。`Ctrl+4` と同じ自動タグ。クリップボードは触らない）。`| set a` は変数へ残す。`| open` は URL かパスなら開く。`| show` はヘルプ画面に出す。`"` の中の `|` では切らない

### あとまわし

- [ ] ブラウザの貼り付け先をウィンドウタイトルではなく URL で覚えたい。UI Automation が要るので様子見

## やらないこと

- `I` で選択範囲を履歴本文に置き換えること。アプリによって効かないのでやらない
- `3dd` や `5j` のカウント接頭辞。`1`〜`9` を直接貼り付けに使っているので入れられない。繰り返しは `.` で足りる
- 本文のコードフェンス（三つバッククォートで囲んだブロック）を実行の目印にすること。コピー元の文章に普通に混ざるので、意図しない実行の経路になる
- `"a yy` で a に、`"a p` で a から貼る名前付きレジスタ。`"` の次の 1 文字（`a`〜`z`）を待つ。`"` を付けなければ今までのレジスタ。アプリ終了で忘れる
- 全角半角・ひらがなカタカナを正規化して検索したい
- ローマ字検索で日本語に当てたい
- `Ctrl+4` で登録するとき、本文が JWT（`eyJ` で始まるドット区切り）、`-----BEGIN `、`sk-`、`ghp_`、`github_pat_` のどれかに当たれば `#secret` を自動で付ける。外すのは今どおり `T`
- `#os:windows` / `#os:linux` で OS ごとに行を出すこと。`#app:` と `{{when}}` で足りる
- `#clip` で Enter をクリップボード置きにすること。末尾 `| clip`（`> clip` も同じ）で足りる
- `#page:GitHub` でウィンドウタイトルのページ名に絞ること。`#app:` と検索で足りる
- `gx`。開く系は `:` コマンド（と必要なら `:map`）で足りる
- 貼り付けキュー（`V` の Enter で 1 件ずつ順に貼る）。今は改行つなぎで足りる
- `#key:` で行ごとにグローバルショートカット。`#slot:n` と `:map` で足りる
- `ma` / `'a` のジャンプ用マーク。検索とピンで足りる
